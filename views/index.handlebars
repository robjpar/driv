<nav class="navbar navbar-expand-lg sticky-top navbar-dark bg-dark text-light">
     <img id="drivloginimg" src="/styles/drivlogo.png">
</nav>

<div id="indexcontainer" class="container p-3 shadow-sm">
  <div class="row">
    <div class="col-12">
      <h2> {{msg}} <span id="headtext">Select your criteria and click "Search"</span></h2>

      <form>

        <div class="col-4 border-right border-info">
          <div class="">
            <h5>Referral Type</h5>
            <div class="form-group">
              <select class="form-control" id="referral-type">
                <option>Referred donors</option>
                <option>Missed potential donors</option>
              </select>
            </div>
          </div>

          <div class="">
            <h5>Enter Age</h5>
            <div class="row">
              <div class="form-group col">
                <label for="min-age-input" class="col col-form-label">Min</label>
                <input class="form-control" type="number" value="" id="min-age-input">
              </div>
              <div class="form-group col">
                <label for="max-age-input" class="col col-form-label">Max</label>
                <input class="form-control" type="number" value="" id="max-age-input">
              </div>
            </div>
          </div>

          <h5>Follow-up</h5>
          <div class="form-check" id="needs-followup">
            <input class="form-check-input" type="checkbox" value="" id="needs-followup">
            <label class="form-check-label" for="needs-followup">
              Include cases that need follow-up?
            </label>
          </div>

          <div class="">
            <h5>Choose Hospital/s</h5>
            <div class="form-check" id="hospitals">
              {{!-- <input class="form-check-input" type="checkbox" value="" id="example">
                  <label class="form-check-label" for="hospitals">
                    Default checkbox
                  </label> --}}
            </div>

          </div>
        </div>
        <button id="submit" class="btn btn-info mt-2">Search</button>
      </form>

      {{!-- <h3>Examples:</h3>
        <ul id="example-list" class="list-group">
          {{#each examples}}
          <li data-id="{{this.id}}" class="list-group-item">
            <a href="example/{{this.id}}">{{this.text}}</a>
            <button class="btn btn-danger float-right delete">ｘ</button>
          </li>
          {{/each}}
        </ul> --}}

    </div>
  </div>

    <div class="col-8">
      <h5>Search Results</h5>
      {{!-- <ul id="referral-info" class="list-group">
        {{#each referrals}}
        <li data-id="{{this.id}}" class="list-group-item">
          <a href="example/{{this.id}}">{{this.text}}</a>
        </li>
        {{each}}
      </ul> --}}
    </div>

</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/index.js"></script>

{{!-- var slider = document.getElementById('slider');

noUiSlider.create(slider, {
    start: [20, 80],
    connect: true,
    range: {
        'min': 0,
        'max': 100
    }
}); --}}