<div id="home">
  <div class="header">
  <a href="#default" class="logo"><i class="fas fa-band-aid"></i>CompanyLogo</a>
  <div class="header-right">
    <a class="active" href="#home">Home</a>
    <a href="#contact">Contact</a>
    <a href="#about">About</a>
  </div>
</div>
  <div class="container">
    <div class="row">
      <div class="col-8 offset-2">
        <h1>{{msg}}</h1>
        <h2>D.R.I.V.</h2>

          <form>
            <div class="form-group">
              <label for="referral-type">Referral Type</label>
              <select class="form-control" id="referral-type">
                <option>Referred donors</option>
                <option>Missed potential donors</option>
              </select>
            </div>
              Age: <b>0</b> <input id="age" type="text" class="span2" value="" data-slider-min="0" data-slider-max="80" data-slider-step="1" data-slider-value="[250,450]"/> <b>80</b>

            <div class="form-check" id="hospitals">
              <input class="form-check-input" type="checkbox" value="" id="hospitals">
              <label class="form-check-label" for="hospitals">
                Default checkbox
              </label>
            </div>>
          <button id="submit" class="btn btn-primary float-right">Submit</button>
        </form>
        <h3>Examples:</h3>
        <ul id="example-list" class="list-group">
          {{#each examples}}
          <li data-id="{{this.id}}" class="list-group-item">
            <a href="example/{{this.id}}">{{this.text}}</a>
            <button class="btn btn-danger float-right delete">ｘ</button>
          </li>
          {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/index.js"></script>

{{!-- var slider = document.getElementById('slider');

noUiSlider.create(slider, {
    start: [20, 80],
    connect: true,
    range: {
        'min': 0,
        'max': 100
    }
}); --}}
